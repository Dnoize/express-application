{{#each movies as |movie|}}
  <div class="movie">
    <figure class="movie-poster">
      {{#if movie.fields.image_url}}
      <img src="{{movie.fields.image_url}}" alt="{{movie.fields.title}}"> 
      {{else}}
      <img src="https://www.valmorgan.com.au/wp-content/uploads/2016/06/default-movie-1-3.jpg" alt=""> 
      {{/if}}
    </figure>
    <div class="movie-title">
      <a href="/review/{{movie.fields._id}}">{{movie.fields.title}}</a>
    </div>
    <p>{{movie.fields.plot}}</p>
  </div>
{{/each}}